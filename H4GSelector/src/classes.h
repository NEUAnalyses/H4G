#include <vector>
#include <map>
#include <algorithm>
#include <string>
#include <utility>

namespace {
	struct dictionary {
		std::vector<std::map<std::string,float>> dummy_idvars;
		std::vector<std::map<std::string,int>> dummy_idbools;
	};
}
